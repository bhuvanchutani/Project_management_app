# Dev: leave unset — Vite proxies /api to localhost:8081 (see vite.config.js).
# Railway / production build: set to your backend public URL + /api, e.g.:
# VITE_API_BASE_URL=https://your-backend.up.railway.app/api
